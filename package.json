{
  "name": "@gi-tcg/gts",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">= 26.0.0"
  },
  "packageManager": "pnpm@11.5.2",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "publish": "pnpm build && pnpm -r publish --access public --no-git-checks"
  },
  "repository": "https://github.com/piovium/gts.git",
  "license": "Apache-2.0",
  "devDependencies": {
    "@gi-tcg/gtsc": "workspace:*",
    "@gi-tcg/unplugin-gts": "workspace:*",
    "@gi-tcg/prettier-plugin-gts": "workspace:*",
    "bunup": "^0.16.26",
    "tsdown": "^0.22.0",
    "typescript": "catalog:"
  },
  "dependencies": {
    "vitest": "^4.1.6"
  }
}
