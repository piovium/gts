{
  "properties": {
    "gamingTs": {
      "title": "GamingTS transpile options",
      "type": "object",
      "properties": {
        "runtimeImportSource": {
          "type": "string",
          "default": "@gi-tcg/gts-runtime",
          "description": "Where to import GamingTS runtime from."
        },
        "providerImportSource": {
          "type": "string",
          "default": "@gi-tcg/core/gts",
          "description": "Where to import semantic providers, including `rootVM` and `binder`."
        }
      }
    }
  }
}
